<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Exercise | QuizPi</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-color: #4e73df;
            --secondary-color: #f8f9fc;
            --accent-color: #2e59d9;
            --text-color: #5a5c69;
            --light-gray: #f8f9fc;
            --dark-gray: #d1d3e2;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--secondary-color);
            color: var(--text-color);
        }
        
        .view-container {
            max-width: 900px;
            margin: 2rem auto;
        }
        
        .exercise-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            transition: transform 0.3s ease;
        }
        
        .exercise-card:hover {
            transform: translateY(-5px);
        }
        
        .card-header {
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            color: white;
            padding: 1.5rem;
            border-bottom: none;
        }
        
        .card-body {
            padding: 2rem;
        }
        
        .detail-item {
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
        }
        
        .detail-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .detail-label {
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
        }
        
        .detail-label i {
            margin-right: 0.5rem;
            font-size: 1.1rem;
        }
        
        .detail-value {
            font-size: 1.1rem;
            padding-left: 1.75rem;
        }
        
        .options-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        
        .options-list li {
            padding: 0.5rem 0;
            border-bottom: 1px dashed var(--dark-gray);
        }
        
        .options-list li:last-child {
            border-bottom: none;
        }
        
        .badge-pill {
            padding: 0.5em 1em;
            font-size: 0.9rem;
            font-weight: 500;
        }
        
        .action-btn {
            border-radius: 50px;
            padding: 0.5rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .back-btn {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
        }
        
        .back-btn:hover {
            background-color: var(--primary-color);
            color: white;
        }
        
        .delete-btn {
            background-color: #e74a3b;
            border: none;
        }
        
        .delete-btn:hover {
            background-color: #c03526;
            transform: translateY(-2px);
        }
        
        .modal-header {
            background: linear-gradient(135deg, #e74a3b, #c03526);
            color: white;
        }
    </style>
</head>

<body>
    {% include 'partials/navbar.html.twig' %}

    <div class="container view-container">
        <div class="exercise-card">
            <div class="card-header">
                <h2 class="mb-0">
                    <i class="fas fa-question-circle me-2"></i>
                    Exercise Details
                </h2>
            </div>
            
            <div class="card-body">
                <div class="row">
                    <div class="col-md-12 detail-item">
                        <div class="detail-label">
                            <i class="fas fa-question"></i> Question
                        </div>
                        <div class="detail-value">
                            {{ exercice.question }}
                        </div>
                    </div>
                    
                    <div class="col-md-12 detail-item">
                        <div class="detail-label">
                            <i class="fas fa-list-ul"></i> Options
                        </div>
                        <div class="detail-value">
                            {% if exercice.options %}
                                <ul class="options-list">
                                    {% for option in exercice.options|split(',') %}
                                        <li>{{ option }}</li>
                                    {% endfor %}
                                </ul>
                            {% else %}
                                <span class="text-muted">No options provided</span>
                            {% endif %}
                        </div>
                    </div>
                    
                    <div class="col-md-6 detail-item">
                        <div class="detail-label">
                            <i class="fas fa-star"></i> Score
                        </div>
                        <div class="detail-value">
                            {{ exercice.score }} points
                        </div>
                    </div>
                    
                    <div class="col-md-6 detail-item">
                        <div class="detail-label">
                            <i class="fas fa-check-circle"></i> Correct Answer
                        </div>
                        <div class="detail-value">
                            {{ exercice.correctAnswer }}
                        </div>
                    </div>
                    
                    <div class="col-md-6 detail-item">
                        <div class="detail-label">
                            <i class="fas fa-image"></i> Image Path
                        </div>
                        <div class="detail-value">
                            {{ exercice.imagePath }}
                        </div>
                    </div>
                    
                    <div class="col-md-6 detail-item">
                        <div class="detail-label">
                            <i class="fas fa-exclamation-circle"></i> Mandatory
                        </div>
                        <div class="detail-value">
                            <span class="badge rounded-pill bg-{{ exercice.isMandatory ? 'success' : 'secondary' }}">
                                {{ exercice.isMandatory ? 'Required' : 'Optional' }}
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a 
               class="btn back-btn action-btn">
                <i class="fas fa-arrow-left me-2"></i> Back to Exercises
            </a>
            
            <div>
                <a 
                   class="btn btn-primary action-btn me-2">
                    <i class="fas fa-edit me-2"></i> Edit
                </a>
                
                <button type="button" class="btn delete-btn action-btn" data-bs-toggle="modal" data-bs-target="#deleteModal">
                    <i class="fas fa-trash me-2"></i> Delete
                </button>
            </div>
        </div>
    </div>



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const card = document.querySelector('.exercise-card');
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';
            
            setTimeout(() => {
                card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                card.style.opacity = '1';
                card.style.transform = 'translateY(0)';
            }, 100);
        });
    </script>
</body>
</html>