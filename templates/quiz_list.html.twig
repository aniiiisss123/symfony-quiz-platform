<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Liste des Quiz{% endblock %}</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    
    <style>
        h1.mb-4 {
            text-align: center;
            font-size: 2.5rem;
            font-weight: bold;
            color: #2c3e50;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 20px;
            padding: 20px;
            background-color: #f1f1f1;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .action-buttons .btn {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
            margin-right: 0.25rem;
        }
        
        .table-responsive {
            overflow-x: auto;
        }
        
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(0, 0, 0, 0.02);
        }
        
        .table-hover tbody tr:hover {
            background-color: rgba(0, 0, 0, 0.05);
        }
        
        .note-cell {
            font-weight: bold;
            color: #2e59d9;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .stats-section {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-bottom: 30px;
        }

        .stat-card {
            flex: 1;
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .stat-card .icon {
            font-size: 3rem;
            color: #4e73df;
            margin-bottom: 10px;
        }

        .stat-card h4 {
            font-size: 1.5rem;
            color: #2c3e50;
            margin-bottom: 10px;
        }

        .stat-card p {
            font-size: 1.25rem;
            color: #555;
            margin: 0;
        }

        .table-dark {
            background-color: #343a40;
            color: white;
        }
    </style>
</head>
<body>
    {% include 'partials/navbareleve.html.twig' %}

    <div class="container py-4">
        <h1 class="mb-4">Mes Quiz</h1>
        
        <div class="stats-section">
            <div class="stat-card">
                <div class="icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                <h4>Total des Quiz Passés</h4>
                <p>{{ passedQuizCount }}</p>
            </div>
          <div class="stat-card">
    <div class="icon">
        <i class="fas fa-chart-line"></i>
    </div>
    <h4>Moyenne des Scores (%)</h4>
    <p>{{ percentage|number_format(2, '.', ' ') }} %</p>
    <p>
        {% if percentage >= 60 %}
            <span class="badge bg-success text-light">
                <i class="fas fa-medal"></i> Good
            </span>
        {% elseif percentage <= 50 %}
            <span class="badge bg-bronze text-light" style="background-color: #cd7f32;">
                <i class="fas fa-medal"></i> Medium
            </span>
        {% else %}
            <span class="badge bg-dark text-light">
                <i class="fas fa-times-circle"></i> No Badge
            </span>
        {% endif %}
    </p>
</div>
        </div>

        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Titre</th>
                        <th>Description</th>
                        <th>Durée (min)</th>
                        <th>Date</th>
                        <th>Note</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for quiz in quizzes %}
                    <tr>
                        <td>{{ quiz.title }}</td>
                        <td>{{ quiz.description }}</td>
                        <td>{{ quiz.duration }}</td>
                        <td>{{ quiz.creationdate|date('d/m/Y H:i') }}</td> 
                        <td class="note-cell">
                            {% set note = notes[quiz.quiz_id] ?? null %}
                            {% set totalScore = totalScores[quiz.quiz_id] %}
                            {{ note ? note.note_totale ~ '/' ~ totalScore : 'Non noté' }}
                        </td>
                        <td class="action-buttons">
                            {% if note %}
                                <button class="btn btn-dark btn-sm" disabled title="Vous avez déjà passé ce quiz">
                                    <i class="fas fa-check-circle"></i> Déjà passé
                                </button>
                            {% else %}
                                <a href="{{ path('app_quiz_take', {'quiz_id': quiz.quiz_id}) }}" 
                                   class="btn btn-primary btn-sm">
                                    <i class="fas fa-play-circle"></i> Passer
                                </a>
                            {% endif %}
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="6" class="text-center">Aucun quiz disponible</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>