<form method="post" action="{{ path('app_quiz_delete', {'quiz_id': quiz.quiz_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz.quiz_id) }}">
    <button class="btn">Delete</button>
</form>
